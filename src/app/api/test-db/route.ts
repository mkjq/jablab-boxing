import { NextResponse } from 'next/server'; import { prisma } from '@/lib/prisma'; export const runtime = 'edge'; export async function GET() { try { const count = await prisma.coach.count(); return NextResponse.json({ success: true, count, env: !!process.env.DATABASE_URL }); } catch (e) { return NextResponse.json({ success: false, error: String(e), env: !!process.env.DATABASE_URL }); } }